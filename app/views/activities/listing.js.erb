$(".activity-list").append("<%= j render @activities %>");

<% if @activities.next_page %>
  $(".pagination").replaceWith('<%= j will_paginate @activities,
    class: "hide pagination" %>');
<% else %>
  $("#load_more").remove();
  $(".pagination").remove();
<% end %>
